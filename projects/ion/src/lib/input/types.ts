export interface IonInputProps {
  /**
   * Indicates whether the input is invalid.
   * @type {string}
   * @default false
   * @example
   * <input [invalid]="true" ionInput />
   */
  invalid?: boolean;
}
